# SpotifyAPITopTracks
This project displays the top 10 most-streamed songs of the month from a selected region using the Spotify API. The application uses a small database to store and visualize the data with Blazor and C#.
